# Web Services

Django project with Django REST Framework for building web services.

## Quick Start

```bash
git clone https://github.com/netkaruma/web-services
cd web-services
pip install django djangorestframework
python manage.py runserver
